'use server'

export async function getAgencies() {
  // const data = await api.get('/agencies').then((res) => res.data)

  // Orderna os dados para o header da tabela
  // const response = data.map((user: UserType) => {
  return {
    // id: user.id,
    // imageUrl: user.imageUrl,
    // name: user.name,
    // email: user.email,
    // status: user.status,
  }
  // })

  // return response
}
